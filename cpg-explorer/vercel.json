{
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "ignoreCommand": "git diff --quiet HEAD -- frontend src",
  "env": {
    "VITE_API_URL": "@api_url"
  }
}
